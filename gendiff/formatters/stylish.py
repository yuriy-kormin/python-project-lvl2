def make_out_format(data):
    if type(data) is bool:
        return str(data).lower()
    elif data is None:
        return 'null'
    else:
        return str(data)


def stylish(data):
    delimiter = '    '
    def make_space(element):
        if element['diff'] == 'equal'
    def inner(id, data):
        if id ==0:
           pass
        else:
            result = ['{']
            for child in